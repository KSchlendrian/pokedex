import { useState } from "react";
import Navbar from "../../shared/Navbar/Navbar";

const Details = () => {
  const [allPokemonDetails, setAllPokemonDetails] = useState([]);

  return (
    <>
      <h2>ich bin Details</h2>
    </>
  );
};

export default Details;
